import { redirect } from 'next/navigation'

// Rota órfã consolidada em /minha-clinica. Sprint 2 mini pré-beta.
// Conteúdo original preservado no histórico do git.
export default function Page() {
  redirect('/minha-clinica')
}
