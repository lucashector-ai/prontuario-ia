// Re-export do Card canônico (components/ui).
// Mantido apenas para compatibilidade com imports antigos
// `import { Card } from '@/components/Card'`. Prefira `@/components/ui`.
export { Card } from './ui'
